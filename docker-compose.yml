services:
  dashboard:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./backend/x_session.json:/app/backend/x_session.json
    environment:
      - PORT=8080
      - PYTHONUNBUFFERED=1
    restart: always
