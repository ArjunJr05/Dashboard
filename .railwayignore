# Railway Ignore — keeps upload small to prevent timeout

# ── Node ──────────────────────────────────────────────────
node_modules/
node_modules
npm-debug.log*

# ── Python virtualenvs (ALL locations) ───────────────────
.venv/
.venv
venv/
venv
env/
backend/.venv/
backend/.venv
*/.venv/
*/.venv

# ── Python cache ─────────────────────────────────────────
__pycache__/
__pycache__
*/__pycache__/
*/__pycache__
backend/__pycache__/
*.pyc
*.pyo
*.pyd

# ── Build artifacts ───────────────────────────────────────
dist/
dist-ssr/
.cache/
client/
public/dist/

# ── Logs ─────────────────────────────────────────────────
*.log
build.log

# ── Git & IDE ─────────────────────────────────────────────
.git/
.git
.vscode/
.idea/
.gemini/

# ── Catalyst ─────────────────────────────────────────────
.catalystrc
catalyst.json
catalyst_appsail/

# ── Binary / archive files ────────────────────────────────
*.zip
*.tar
*.tar.gz
*.gz
*.exe
*.bin

# ── NOTE: data.json files are intentionally NOT ignored ──
# backend/data.json is baked into the Docker image and serves as
# the seed/fallback when /tmp is wiped after a Railway restart.
# The Dockerfile COPY backend/ ./ already includes it.

# NOTE: backend/x_session.json is intentionally included — needed at startup
