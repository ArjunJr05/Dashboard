{
	"command": "gunicorn --bind 0.0.0.0:$X_ZOHO_CATALYST_LISTEN_PORT --workers 1 --threads 4 --timeout 0 server:app",
	"build_path": ".",
	"stack": "custom",
	"docker_file": "Dockerfile",
	"env_variables": {
		"ENABLE_CATALYST_UPLOAD": "1",
		"PLAYWRIGHT_BROWSERS_PATH": "/app/pw-browsers",
		"HOME": "/root"
	},
	"memory": 2048,
	"scripts": {}
}
